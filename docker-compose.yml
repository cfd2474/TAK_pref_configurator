services:
  tak-pref-configurator:
    build: .
    container_name: tak-pref-configurator
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_PORT:-8080}:8080"
    environment:
      - PORT=8080
